info:
  name: Register
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8000/auth/register
  body:
    type: json
    data: |
      {
        "name": "Test User",
        "email": "test@test.com",
        "password": "password123",
        "role": "user"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
