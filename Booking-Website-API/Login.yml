info:
  name: Login
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8000/auth/login
  body:
    type: json
    data: |
      {
        "email": "test@test.com",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
